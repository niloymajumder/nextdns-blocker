# Production dependencies
# Pin to specific minor versions for stability while allowing patch updates

requests>=2.28.0,<3.0.0

# Timezone support:
# - Python 3.9+ uses built-in zoneinfo module
# - Python 3.8 requires backports.zoneinfo
# We use tzdata for up-to-date timezone database on all platforms
tzdata>=2023.3

# Python 3.8 compatibility - zoneinfo backport
# This is only installed on Python < 3.9
backports.zoneinfo>=0.2.1;python_version<"3.9"
